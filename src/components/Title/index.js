import React from 'react';
import styles from './styles.module.scss';

const Title = () => {
    return (
        <>
            <h1 className={styles.title__main}>work</h1>
        </>
    );
};

export default Title;
